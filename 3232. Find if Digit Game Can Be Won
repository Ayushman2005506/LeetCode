class Solution {
public:
    bool canAliceWin(vector<int>& nums) {
        int count1=0;
        int count2=0;
        for(int i=0;i<nums.size();i++){
            if(nums[i] >= 0 && nums[i] <= 9){
                count1+=nums[i];
            }
            else{
                count2+=nums[i];
            }
        }
        if(count1>count2){
            return true;
        }
        else if(count1<count2){
            return true;
        }
        else{
            return false;
        }
    }
};
