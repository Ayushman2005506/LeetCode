class Solution {
public:
    int returnToBoundaryCount(vector<int>& nums) {
        int ans=0;
        int count=0;
        for(int x:nums){
            ans+=x;
            if(ans==0){
                count++;
            }
        }
        return count;
    }
};
